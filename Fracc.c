#include <stdio.h>

int main() {
    double resultado = (1.0/3.0 + 3.0/5.0 + 1.0/30.0) / (23.0/30.0 + 0.2229);
    double resultadob = 5 + (2.0) / (1.0 / (((2.0) / 2.0 - 1.0 / 4.0) + 1.39));

    printf("El resultado del a es: %f", resultado);

    printf("\n");

    printf("El resultado del b es %f", resultadob);


    printf("\n");
    system("Pause");
    return 0;
}
